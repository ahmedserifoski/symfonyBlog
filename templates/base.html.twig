<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}

          {# Adding bootsrap for the app to use everywhere!!!!!!!!!!!!!!!!!!!!!!!!!! #}
          
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
        {# https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/5.1.3/js/bootstrap.min.js #}
        {% block stylesheets %}
            {#{{ encore_entry_link_tags('app') }}#}
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
    <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
    {# the name of the annotiation is being used HERE !!!!!!!!!!! --> home #}
  <a class="navbar-brand" href="{{path('home')}}">Home</a>
  <a class="navbar-brand" href="{{path('post.index')}}">Posts</a>
  <a class="navbar-brand" href="{{path('register')}}">Register</a>
  {% if is_granted("IS_AUTHENTICATED_FULLY") %}
      
        <a class="nav-link" href="{{path('app_logout')}}">LogOut</a>
      
      {% endif %}
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      
    </ul>
  </div>
</nav>

{# bootsrtap thing not twig thing #}
    <div class="container">
        {% block body %}{% endblock %}
        
    </div>
    
    </body>
</html>
